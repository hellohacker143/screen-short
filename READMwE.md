# Text to Screenshot (Streamlit App)

This Streamlit app converts entered text into a paragraph view
and allows downloading it as an image (screenshot-style).

## Run locally
pip install -r requirements.txt
streamlit run app.py
